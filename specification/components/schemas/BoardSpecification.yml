type: object
additionalProperties: false
description: |
  Minimal board specification.
required:
  - stopPlaceRef
  - serviceDeparture
properties:
  stopPlaceRef:
    $ref: ./StopPlaceRef.yml
  replacedPlaceRef:
    description: >-
      reference to the original place in case of a replacement (e.g. bus stop of
      a replacement bus for a train)
    $ref: ./StopPlaceRef.yml
  serviceDeparture:
    $ref: ./ServiceTime.yml
