type: object
additionalProperties: false
properties:
  problems:
    type: array
    items:
      $ref: ./Problem.yml
  exchangeOffers:
    type: array
    items:
      $ref: ./ExchangeOffer.yml
  trips:
    description: |
      Two trips are returned if  round trips are exchanged.
    type: array
    items:
      $ref: ./Trip.yml
  tripContext:
    $ref: ./TripContext.yml
  passengers:
    type: array
    items:
      $ref: ./Passenger.yml
  _links:
    $ref: ./Links.yml
