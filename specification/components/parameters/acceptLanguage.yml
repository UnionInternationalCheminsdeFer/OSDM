name: Accept-Language
in: header
description: >
  Describes the set of natural languages that are preferred for localized text
  in the response

  to the request (see RFC9110). Supporting English (en) is a must.
schema:
  type: string
