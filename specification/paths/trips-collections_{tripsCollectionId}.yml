get:
  tags:
    - Trips
  summary: Returns a collection of trips.
  description: >
    Returns a collection of trips for a for a given trips collection id.

    The unique codes of the origin and destination can be resolved using the
    places service.
  operationId: getTripsCollectionId
  parameters:
    - $ref: ../components/parameters/requestor.yml
    - $ref: ../components/parameters/acceptLanguage.yml
    - $ref: ../components/parameters/traceParent.yml
    - $ref: ../components/parameters/traceState.yml
    - $ref: ../components/parameters/acceptNamespace.yml
    - $ref: ../components/parameters/page.yml
    - name: tripsCollectionId
      in: path
      description: |
        id of the trips
      required: true
      schema:
        type: string
    - name: stopBehavior
      in: query
      schema:
        $ref: ../components/schemas/StopBehavior.yml
      required: false
    - name: embed
      in: query
      description: >
        Influences whether referenced resources are returned in full or as
        references only.

        Default value: ALL
      schema:
        type: array
        maxItems: 2
        items:
          $ref: ../components/schemas/TripsCollectionResponseContent.yml
  responses:
    '200':
      description: |
        trips found
      content:
        application/vnd.uic.osdm+json:
          schema:
            $ref: ../components/schemas/TripCollectionResponse.yml
      headers:
        Content-Language:
          schema:
            type: string
            description: >
              The language of translatable strings in the response (see
              RFC2616-sec14.12).
    '400':
      $ref: ../components/responses/BadRequestResponse.yml
    '401':
      $ref: ../components/responses/UnauthorizedResponse.yml
    '403':
      $ref: ../components/responses/ForbiddenResponse.yml
    '404':
      $ref: ../components/responses/NotFoundResponse.yml
    '415':
      $ref: ../components/responses/UnsupportedMediaTypeResponse.yml
    '500':
      $ref: ../components/responses/InternalServerErrorResponse.yml
    '501':
      $ref: ../components/responses/NotImplementedResponse.yml
    '503':
      $ref: ../components/responses/ServiceUnavailableResponse.yml
    default:
      $ref: ../components/responses/DefaultErrorResponse.yml
  security:
    - oAuth2ClientCredentials: []
